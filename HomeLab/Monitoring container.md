

- Monitoring should monitor the cluster and be able to run simple benchmarks with a click like network speed or vine bench type shit should later monitor 
- want a screen where i can see the loads on each of my nodes for memory cpu and gpu (only for d1) usage, the temps (cpu temp, gpu temp, other sensors that are available)
- have some sort of scheduled benchmarking that does simples benchmarks and saves data and displays it like network speed